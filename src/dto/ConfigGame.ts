export interface ConfigGame {
  normalAttack: number;
  cooldownSkill3: number;
  enemysRespawnTime: number;
  amountProtectBall: number;
  speedProtectBall: number;

  start: boolean;
  pause: boolean;
  stop: boolean;
}
